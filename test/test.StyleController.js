// Generated by CoffeeScript 1.9.1
var BaseController, Injector, ioc;

BaseController = require('../backend/controllers/BaseController');

ioc = require('./ioc');

Injector = require('di').Injector;

describe('StyleController:', function() {
  beforeEach(function() {
    return ioc.resolve('controllers.StyleController').bindOn(this);
  });
  afterEach(function() {
    return this.afterEach();
  });
  it("must have actions", function() {
    return this.mod.actions.should.be.an.instanceOf(BaseController);
  });
  it('sets the post update hook', function() {
    return this.mod.actions.postUpdateHook.should.equal(this.mod.postUpdateHook);
  });
  it("must have hasListOwner set to no", function() {
    return this.mod.actions.hasListOwner.should.be["false"];
  });
  return it("must not have a $create action", function() {
    return should.not.exist(this.mod.actions.$create);
  });
});
