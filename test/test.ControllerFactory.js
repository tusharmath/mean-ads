// Generated by CoffeeScript 1.9.1
var CampaignController, ExperimentController, Injector, MongooseProviderMock, Provide, Q, TransientScope, ref;

CampaignController = require('../backend/controllers/CampaignController');

ExperimentController = require('../backend/controllers/ExperimentController');

MongooseProviderMock = require('./mocks/MongooseProviderMock');

ref = require('di'), Injector = ref.Injector, Provide = ref.Provide, TransientScope = ref.TransientScope;

Q = require('q');

describe('ControllerFactory:', function() {
  beforeEach(function() {
    this.injector = new Injector([MongooseProviderMock]);
    this.mod = this.injector.getModule('factories.ControllerFactory');
    return this.mongo = this.injector.getModule('providers.MongooseProvider', {
      mock: false
    });
  });
  afterEach(function() {
    return this.mongo.__reset();
  });
  it("contains an instance of Campaign", function() {
    return this.mod.Controllers.Campaign.should.be.an.instanceOf(CampaignController);
  });
  return it("contains an instance of Experiement", function() {
    return this.mod.Controllers.Experiment.should.be.an.instanceOf(ExperimentController);
  });
});
