// Generated by CoffeeScript 1.9.1
var DbConnection, Injector, Mock;

DbConnection = require('../backend/connections/DbConnection');

Mock = require('./mocks');

Injector = require('di').Injector;

describe('DbConnection:', function() {
  beforeEach(function() {
    this.injector = new Injector(Mock);
    return this.mod = this.injector.get(DbConnection);
  });
  return it("no public mongoose", function() {
    return should.not.exist(this.mod.mongoose);
  });
});
