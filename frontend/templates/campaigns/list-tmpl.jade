div.panel.panel-default
	.panel-heading
		.btn-group
			a.btn.btn-default(ng-href="#/campaigns/create") Create
		.clearfix
	.panel-body
		table.table
			thead
				tr.text-capitalize
					th Campaign
					th Program
					th Duration
					th Commitment
					th.text-right Keys
					th.text-right Actions
			tbody
				tr(ng-repeat="campaign in ctrl.campaigns")
					td
						.text-capitalize {{campaign.name}}
					td
						a.text-capitalize(ng-href="#/programs/{{campaign.program._id}}/update") {{campaign.program.name}}
					td
						strong.text-muted {{campaign.days}}
						small.text-muted &nbsp;days
					td
						strong.text-capitalize.text-muted {{campaign.commitment || "0"}}
						small.text-muted &nbsp;{{campaign.program.gauge}}
					td.text-right
						strong.text-muted {{campaign.keywords.length}}
					td.text-right
						button.btn.btn-xs(type="button"
							ng-click="ctrl.toggleStatus(campaign)"
							ng-class="{'btn-warning': campaign.isEnabled, 'btn-primary': !campaign.isEnabled}")
							span(ng-show="campaign.isEnabled") Stop
							span(ng-hide="campaign.isEnabled") Start
						= ' '
						a.btn.btn-default.btn-xs(ng-href="#/campaigns/{{campaign._id}}/update") Update
						= ' '
						a.btn.btn-default.btn-xs(ng-href="#/campaigns/{{campaign._id}}/metrics") Metrics
